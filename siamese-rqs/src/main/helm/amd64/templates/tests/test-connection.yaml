apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "siamese-rqs.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "siamese-rqs.name" . }}
    helm.sh/chart: {{ include "siamese-rqs.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "siamese-rqs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
